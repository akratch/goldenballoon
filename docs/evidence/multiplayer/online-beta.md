# A4/A5 private online evidence

Decision: **GATED by A3 `GO`; no production online race exists.**

| Field | Evidence |
|---|---|
| Pure room reducer | PASS — native CTest covers compatibility, capacity, seat ownership, per-seat votes, deterministic choice, load barrier, results/rematch, reconnect, leader custody, CAS and idempotent retry mutations |
| Fake-adapter launcher lobby | PASS AUTOMATED — shared native/browser C model covers 42 views, 25 actions and all typed recovery states |
| Browser MatchRoom control binding | PASS LOCAL — create/link-or-code join, QR/share, rotation, selection, Ready, reconnect and malformed-state recovery; service admission cannot expose Start |
| Release activation boundary | PASS LOCAL — disabled policy is lazy/zero-room-I/O; enabled policy requires same origin, clean provenance and locally SHA-verified US/PAL ROM; dirty/cross-origin inputs fail closed |
| Two-profile first-time journey | PASS AUTOMATED / HUMAN OPEN — isolated profiles use the real local Worker for create/share, role-link join, outage/retry, distinct selections, Ready backtrack/reconvergence, AX and leave; uncoached human timing/cancel/rematch study remains |
| Compatibility/direct graph/reconnect | GATED |
| Security/privacy/red-team | GATED |
| Load/chaos/regional/deploy rollback | PARTIAL LOCAL — real Worker passes 64-way exhausted admission, 64-way forged-control rejection with zero reserve use, persisted process restart, weighted HTTP/socket control reserve, lifetime-bounded signaling, static/security-header fallback and literal-zero kill switch. Credentials are room/role-bound before budget/object access. State-only MatchRoom sockets byte-bound hostile UTF-8 and binary frames before close; an allocation-free UTF-8 scanner also protects Phone Party signaling. A 24,576-command seeded reducer campaign requires deterministic mirror execution, valid bounded state, atomic rejection, exact duplicate receipts and reconstruction parity. Secret-gated no-store telemetry reports only exact daily aggregates, latches refusal once and has tested 32-day deletion. Fixed reservation buckets piggyback existing writes, reconstruct admitted units exactly, isolate old-Worker traffic in a legacy bucket and add no flood-amplified metrics write. Two-release Chromium covers waiting-worker skew and atomic offline activation. All 15 Worker/object calls use v1; four legacy readers and unknown-version pre-storage negatives pass. The checked Free-plan edge payload applies its one 30/10-second/IP rule only to `/api/`; static play is excluded and HTML `429` is typed. A strict 64-KiB-bounded reconciliation gate rejects private fields, internal arithmetic drift, paid billing/overages, nonzero charge, changed Free ceilings and 75% stop lines without reflecting input. The v2 ledger adds fixed aggregate-only synthetic MatchRoom create/join and Phone Party direct/input success-latency thresholds without a server metrics path. The 2026-08-12 validation passes 194/194 native CTests, 40/40 service tests, the real-Worker capacity/health gate and edge/internal-API checks. The controlled hosted canary, edge/version/rollback, real provider exports, distributed raw-request exhaustion, distributed/network chaos and hosted evidence remain |
| Seven-day weighted quota/currency ledger | V2 CONTRACT + COLLECTOR PASS LOCAL / HOSTED EVIDENCE GATED — exact bounded validator rejects noncontiguous dates, billing/charge, capacity/provider drift, partial legacy tracking, failed local play, synthetic experience under-sampling/success/latency misses, open incidents and non-GO state. The collector restricts origins, creates fresh profiles, drives production HTTP/WebSocket/WebRTC paths and writes only a new fixed aggregate; a one-attempt local smoke succeeds but is correctly non-qualifying. The separate signed human record remains required and fixture/local days never count. |
| Local/Phone Party failure isolation | GATED |
| Reviewer/date/decision | OPEN |
